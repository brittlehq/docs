node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Frame stills generated by `remotion still` for layout sanity checks.
# These are scratch artifacts, not source.
still-*.png
m-*.png
