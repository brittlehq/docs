---
title: Self-host overview
description: The moving parts of a Brittle deployment and what each piece is responsible for.
---

A Brittle deployment is the Hub container, Postgres, and an artifact
store (local volume in dev; S3/R2 in prod).

## What's required vs optional

| Component        | Required? | Notes                                                                       |
| ---------------- | --------- | --------------------------------------------------------------------------- |
| Hub container    | Yes       | `ghcr.io/brittlehq/brittle:latest`. The only image you need to pull.        |
| Postgres         | Yes       | Version 14+. Single instance is fine; bring your own or use the compose-supplied one. |
| Local volume     | Default   | For artifact bytes. Survives `docker compose down`; dropped on `down -v`.   |
| S3 / R2 bucket   | Optional  | Swap-in for the local volume when you need shared storage across replicas or a CDN-fronted artifact path. |
| Caddy / reverse proxy | Optional | For TLS termination and a clean public domain. The example stack ships one. |

## What you don't need

A few things you might assume are in the stack but aren't:

- **Redis.** Not used today.
- **A separate worker process.** Everything runs inside the Hub
  container.
- **An external auth provider.** Local email/password is built in.
  SSO / OAuth are on the roadmap.

## Two deployment shapes

In rough order of effort:

1. **[Installation](/docs/installation)** — local Docker Compose. The
   canonical few-minute setup.
2. **[Production notes](/docs/self-host/production)** — TLS, real
   domain, S3 artifacts, backups, upgrades.

## Next steps

- [Installation](/docs/installation) — bring the stack up on your
  laptop.
- [Hub configuration](/docs/self-host/configuration) — every config
  field and env var the Hub accepts.
- [Production notes](/docs/self-host/production) — TLS, S3,
  backups, upgrades.
