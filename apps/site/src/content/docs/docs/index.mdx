---
title: Introduction
description: What Brittle is, what it isn't, and whether you should keep reading.
---

Brittle is a self-hosted dashboard for test results. You install one
Docker container, point your test reporter at it, and your CI runs
start arriving as something you can actually read — grouped by branch
and commit, failures clustered by file, flake history per test, and
the video / trace / log payload from each session one click away.

If your CI runs Playwright, WebdriverIO, Jest, or Vitest and you've
ever re-run a failing test just to see it pass, this is built for that
moment.

## What it does

- **Collects results from your existing reporter.** Drop the Brittle
  reporter into your test config next to whatever you already run
  (`list`, `html`, `default`). Each test posts its result, attachments,
  and any annotations to the hub as it finishes.
- **Groups runs by branch and commit.** Each CI invocation becomes one
  Run row. Failures inside that run are grouped by test file, then by
  test name, with `NEW` / `KNOWN` / `REGRESSED` tags so you can ignore
  the ones you've already triaged.
- **Stores the evidence.** Playwright trace, video, HAR, console log,
  network log — anything the reporter captures rides along. All
  addressable from the Session Detail page, all replayable in the
  browser without leaving the dashboard.
- **Tracks each test through time.** A per-test view shows the
  pass/fail ladder over the last 30 days, so a 95%-passing test stops
  reading as green.
- **Optionally clusters failures with AI.** Bring your own provider
  key (Gemini, Anthropic, OpenAI), and the hub groups failure
  signatures into patterns — *"17 tests blew up on the same locator
  selector"* — so triage is one decision per cluster, not one per
  test. Off by default; you opt in per organisation.

## What it isn't

Brittle deliberately stays out of a few lanes:

- **Not a test runner.** Playwright is your runner. WebdriverIO is
  your runner. Brittle reads what your runner emits and stores it
  somewhere browsable.
- **Not a CI system.** GitHub Actions, GitLab CI, Buildkite, Jenkins
  — whatever you have, Brittle plugs into the run, it doesn't
  replace it.

## The shape of the install

One Docker image runs the API + dashboard. Postgres holds metadata;
artifacts live on a local volume (dev) or S3/R2 (prod). A wizard sets
up admin + org + first project; mint a token, paste into your test
config.

## Where to go next

- [**Installation**](/docs/installation) — bring up Brittle locally
  with Docker Compose.
- [**Quickstart**](/docs/quickstart) — add the reporter to an
  existing suite and see your first run.
- [**Reporter configuration**](/docs/reporter/configuration) — every
  option the reporter accepts and what each one does.

:::tip
Want a sandbox with a working test suite already wired up? Clone
[`brittlehq/reporter-examples`](https://github.com/brittlehq/reporter-examples)
— the Hub, dashboard, Postgres, and a working Playwright / WDIO /
Jest / Vitest example come up together with `docker compose up -d`.
:::
